// Không cần JS phức tạp cho page này, nhưng có thể thêm hiệu ứng fade-in nếu muốn
document.addEventListener('DOMContentLoaded', () => {
    document.querySelector('.container').style.opacity = 1;
});